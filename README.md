## To setup:

```bash
npm install
```

touch `.env.local` at project root:

```.env
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
```

^ fill em out.

## To run:

```bash
node server.js
```
and in a separate terminal:

```bash
npm start
```

visit `http://localhost:3000/app` NOTE THE `/app`!!!!
